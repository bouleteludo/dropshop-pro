"use client";
import {useState} from "react";
export function SoundToggle(){const[on,setOn]=useState(false);function toggle(){const next=!on;setOn(next);if(next){const C=window.AudioContext||window.webkitAudioContext;if(C){const ctx=new C();const o=ctx.createOscillator();const g=ctx.createGain();o.frequency.value=880;g.gain.setValueAtTime(.0001,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.06,ctx.currentTime+.02);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+.35);o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+.36);}}}return <button type="button" onClick={toggle} className="fixed bottom-5 left-5 z-50 rounded-full border border-white/15 bg-black/35 px-4 py-2 text-xs text-white/70 backdrop-blur hover:text-white" aria-label="Activer ou désactiver le son">{on?"🔔 Son Noël":"🔕 Son coupé"}</button>}